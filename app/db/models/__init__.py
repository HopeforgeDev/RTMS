# The below are all needed which are imported from doctor_models
from .doctor_models import Doctor
from .doctor_models import Specialization
from .doctor_models import DoctorSpecialization
from .doctor_models import Qualification
from .doctor_models import HospitalAffiliation
# The below are all needed which are imported from client_models
from .patient_models import Patient
# The below are all needed which are imported from admin_models
from .admin_models import Admin
# The below are all needed which are imported from office_models
from .office_models import Office
from .office_models import OfficeDoctorAvailability
from .office_models import InNetworkInsurance
# The below are all needed which are imported from appointment_models
from .appointment_models import Appointment
from .appointment_models import AppointmentStatus

